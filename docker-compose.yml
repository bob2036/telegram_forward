version: '3.8'

services:
  telegram-forwarder:
    build: .
    container_name: telegram-forwarder
    restart: unless-stopped
    
    environment:
      - API_ID=31975468
      - API_HASH=87be4666f1aaa47bb3f7a643793f9bb5
      - PHONE=+33658026889
      - SOURCE_CHAT=@XCommasBot
      - TARGET_CHAT=@n8nph007bot
      - SESSION_PATH=/app/sessions/chat_forward_session
    
    volumes:
      - telegram_sessions:/app/sessions
    
    # Container reste actif pour usage manuel
    command: tail -f /dev/null

volumes:
  telegram_sessions:
    driver: local